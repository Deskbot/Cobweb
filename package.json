{
  "name": "web-server",
  "version": "1.0.0",
  "description": "A web server framework",
  "scripts": {
    "build": "./node_modules/.bin/tsc --incremental",
    "build-watch": "./node_modules/.bin/tsc -w --incremental",
    "build-tests": "./node_modules/.bin/tsc --incremental -p ./test",
    "build-tests-watch": "./node_modules/.bin/tsc -w --incremental -p ./test",
    "rebuild": "./node_modules/.bin/tsc",
    "rebuild-tests": "./node_modules/.bin/tsc -p ./test",
    "test": "./test.sh"
  },
  "author": "",
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^10.0.0",
    "typescript": "^3.6.2"
  },
  "dependencies": {
    "source-map-support": "^0.5.13"
  }
}
